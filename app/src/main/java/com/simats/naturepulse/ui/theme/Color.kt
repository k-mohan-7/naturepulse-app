package com.simats.naturepulse.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Colors — Deep Forest Green & Warm Gold ───────────────────────────

val ForestGreen = Color(0xFF1B4D3E)
val ForestGreenLight = Color(0xFF2E6F40)
val ForestGreenDark = Color(0xFF12372A)
val AccentGreen = Color(0xFF388E3C)
val MintGreen = Color(0xFFA5D6A7)

// Warm Gold / Amber Accents
val WarmGold = Color(0xFFD4AF37)
val WarmAmber = Color(0xFFD9822B)
val LightGold = Color(0xFFFFF8E7)
val GoldBorder = Color(0xFFE6C875)

// ─── Light Surfaces (Crisp & Clean) ─────────────────────────────────────────

val BackgroundDark = Color(0xFFF6F8F6)        // Main screen background (light warm gray-green)
val SurfaceDark = Color(0xFFFFFFFF)           // Cards, top bars, bottom sheet
val SurfaceVariantDark = Color(0xFFF0F4F1)    // Input fields, secondary cards
val CardDark = Color(0xFFFFFFFF)              // Elevated cards

// ─── Text Colors ─────────────────────────────────────────────────────────────

val OnSurface = Color(0xFF1A2621)             // Primary dark text
val OnSurfaceMuted = Color(0xFF4A5D55)        // Secondary text
val OnSurfaceFaint = Color(0xFF7A8E85)        // Muted labels & timestamps

// ─── Semantic Colors ──────────────────────────────────────────────────────────

val ErrorRed = Color(0xFFD32F2F)
val CriticalRed = Color(0xFFC62828)
val SuccessGreen = Color(0xFF2E7D32)
val WarningAmber = Color(0xFFF57C00)

// ─── Status Badge Colors ──────────────────────────────────────────────────────

val StatusOpen = Color(0xFF0288D1)
val StatusPending = Color(0xFFED6C02)
val StatusUnderReview = Color(0xFF9C27B0)
val StatusResolved = Color(0xFF2E7D32)
val StatusClosed = Color(0xFF616161)

// ─── Severity Colors ──────────────────────────────────────────────────────────

val SeverityLow = Color(0xFF388E3C)
val SeverityMedium = Color(0xFFF57C00)
val SeverityHigh = Color(0xFFE65100)
val SeverityCritical = Color(0xFFC62828)

// ─── Type Colors ──────────────────────────────────────────────────────────────

val TypeWildlife = Color(0xFF2E6F40)
val TypePollution = Color(0xFFB71C1C)